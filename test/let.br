fn main -> (Int,Int)
  let x = 0
      y = 4
      y -> add1(y)
      (a,b) as z = (1,2)
      [v, vs] = [1,2,3,4]
  in
    [(x,y), (y,x), (a,b), z, (v,v)]
  end
end

fn foo
  let import Mortar.Graphs.Digraph in x end
end

fn bar
  let x : 'a -> 'b -> Int = fn(_,_) -> 2 in x end
end
